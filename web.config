{
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend/arcsat-landing && npm install && npm run build",
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}
